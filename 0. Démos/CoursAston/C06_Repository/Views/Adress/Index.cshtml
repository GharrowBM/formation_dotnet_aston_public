@model List<Adress>

@{
    ViewData["Title"] = "Liste des adresses";
}

<div class="row">
    <h3>Liste des adresses</h3>
    <hr />
    <table class="table">
        <thead>
            <tr>
                <th>Id.</th>
                <th>Adresse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.FullAdress</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-info">Editer</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Supprimer</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <hr />
    <a class="btn btn-success" asp-action="Create">Nouvelle adresse</a>
</div>
